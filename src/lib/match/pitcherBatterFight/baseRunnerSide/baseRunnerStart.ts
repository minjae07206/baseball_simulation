export function baseRunnerDidStart(record:any) {
    const startArray = [null, null, null]
    if (record.firstbase !== null && record.secondbase === null && record.thirdbase === null) {
        // 주자 1루 상황
        // 어느 정도 baseRunning 과 투수의 홀드 러너 비율을 정해서 뛰는걸 정한다.
        // 뛸 확률(매 카운트마다)
    }
    //풀카운트면 스타트

    // 경기 후반에 점수차이가 3점 나는데 1루에 빠른 주자가 있다고 해서 뛰지 않는다. 주자를 모으는게 더 좋다.

    // 점수차이가 많이 나고, 5회 이상이면 노스타트
    return []
}