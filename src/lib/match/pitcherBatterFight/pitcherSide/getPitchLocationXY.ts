export function getPitchLocationXY (intendedLocationX:number, intendedLocationY:number, command:number, STRIKEZONE_WIDTH:number, STRIKEZONE_HEIGHT:number) {
    const BOX_WIDTH = STRIKEZONE_WIDTH * 3;
    const BOX_HEIGHT = STRIKEZONE_HEIGHT * 3;

    return [0, 0]
}